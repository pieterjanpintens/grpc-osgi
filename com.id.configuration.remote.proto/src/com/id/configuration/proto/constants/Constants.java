package com.id.configuration.proto.constants;

import com.id.configuration.proto.Empty;
import com.id.configuration.proto.ValueOption;

public class Constants {
	public static final ValueOption kNOTSET = ValueOption.newBuilder().build();
	
	public static final Empty kEMPTY = Empty.newBuilder().build();
}
